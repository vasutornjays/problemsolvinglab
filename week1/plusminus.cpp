#include <iostream>

using namespace std;

main()
{
    int n ,sum;
    int input[1100];
    char sign[1100];
    cin >> n;
    for(int i = 0;i < 2*n - 1 ;i++)
    {
        else if(i%2 == 0)
        {
            cin >> input[i];
        }
        else()
        {
            cin >> sign[i];
        }
    }
    sum = input[0];
}